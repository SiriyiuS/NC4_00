;               LL2, ex02
        .org    h'0000
        lxi B, h'3132   ; Загрузка регистровой пары BC числом 3132h.
                        ; Младший байт данных загружается в
                        ; регистр C а старший байт - в регистр В.
        lxi D, h'3334   ; Загрузка регистровой пары DE числом 3334h.
                        ; Младший байт данных загружается в регистр E,
                        ; а старший байт - в регистр D.
        lxi H, h'3536   ; Загрузка регистровой пары HL числом 3536h.
                        ; Младший байт данных загружается в регистр L,
                        ; а старший байт - в регистр Н.
        .end
